# Andrii's Ukulele Shop JSON Processing Script

This Script reads the shop's inventory from a JSON file (data/ukuleles.json),
parses it into a Python object,  adds one category and one item
and writes the result back to JSON (data/ukuleles_updated.json).

## To run
The script uses Python 3.

To execute the script run 
``` python3 main.py```
